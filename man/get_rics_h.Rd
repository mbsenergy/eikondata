% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lowlevel_wrappers.R
\name{get_rics_h}
\alias{get_rics_h}
\title{Retrieve Hourly Time Series Data for a Given RIC}
\usage{
get_rics_h(
  rics,
  from_date = Sys.Date() - (365 * 10),
  to_date = Sys.Date(),
  legacy = getOption("eikondata.legacy", FALSE)
)
}
\arguments{
\item{rics}{A character string representing the base RIC (without the hour suffix) for which the data is to be retrieved.}

\item{from_date}{A character or Date object representing the start date for data retrieval (default: 10 years ago).}

\item{to_date}{A character or Date object representing the end date for data retrieval (default: today).}

\item{legacy}{boolean to use python package (TRUE) or R}

\item{interval}{A character string representing the data frequency. Defaults to `"daily"`, but is internally ignored for hourly data.}

\item{sleep}{A numeric value specifying the time (in seconds) to wait between API calls (default: 0).}
}
\value{
A `data.table` containing the 24-hour data for the specified RIC, with the following columns:
\itemize{
  \item \code{date} - Date of the observation (YYYY-MM-DD).
  \item \code{hour} - Hour of the day (integer from 1 to 24).
  \item \code{ric} - The base RIC identifier.
  \item \code{value} - The retrieved value for the RIC.
  \item \code{volume} - Trading volume or relevant metric.
}
}
\description{
This function retrieves 24-hour time series data for a given Reuters Instrument Code (RIC) by fetching individual hourly data. It returns a `data.table` with structured hourly data for the specified date range.
}
\details{
This function constructs RICs for each of the 24 hours by appending `01` to `24` to the base RIC.
It retrieves the data, processes it into a unified table, and returns a `data.table` with hourly observations.
If not all 24 hours are present, a warning is issued.
}
\note{
If not all 24 hours are present in the dataset, a warning is issued.
}
\examples{
\dontrun{
get_rics_h("HEEGRAUCH", from_date = "2020-01-01", to_date = "2023-01-01")
}

}
